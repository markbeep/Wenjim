{
  "name": "Existing Dockerfile",
  "build": {
    "context": "../backend",
    "dockerfile": "../backend/Dockerfile"
  },
  "postCreateCommand": "pip3 install -U pylint black",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-python.black-formatter"]
    }
  }
}
